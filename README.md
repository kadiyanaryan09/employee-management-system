# employee-management-system
Employee Management System built using React.js, Node.js, Express.js, and MySQL. This project implements complete CRUD operations (Create, Read, Update, Delete) through REST APIs, allowing users to add, view, update, and delete employee records with a responsive user interface and database integration.
